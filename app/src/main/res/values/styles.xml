<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SwitchTheme" parent="Theme.AppCompat.Light">
        <item name="android:colorControlActivated">#009fd4</item>
    </style>

    <!-- Animations -->

    <style name="Animation" />

    <!-- the animations must have been defined in your "anim" folder, of course -->
    <style name="Animation.MyAwesomeAnimation" parent="android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/activity_transition</item>
    </style>

    <style name="Theme.MyAwesomeTheme" parent="@style/AppTheme.NoActionBar">
        <item name="android:windowAnimationStyle">@style/Animation.MyAwesomeAnimation</item>
    </style>

    <!-- Space linear layout style -->

    <style name="StyleLinearLayoutDetails">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginRight">15dp</item>
        <item name="android:layout_marginLeft">15dp</item>
    </style>

    <!-- Login theme -->
    <style  name="TextAppearence.App.TextInputLayout"
            parent="@android:style/TextAppearance">
        <item name="android:textColor">#cbe8f1</item>
    </style>



</resources>
